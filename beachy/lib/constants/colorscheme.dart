import 'package:flutter/material.dart';
//We would call it color scheme but material package already took that name
class AppColors {
  Color primaryColor;
  Color secondaryColor;
  AppColors(
    this.primaryColor,
    this.secondaryColor
  );
}